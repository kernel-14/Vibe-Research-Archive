% Theorem / Definition box — quiet, italicized title, no color fills.
% Requires \usepackage{ra-box-style}
\begin{RATheoremBox}[title={Definition 1 (<short name>)}]
A <thing> is <formal definition>.

We say a <thing> is <property> when <condition>.
\end{RATheoremBox}
