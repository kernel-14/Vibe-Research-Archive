Tracks layout language:

- Two or more horizontal lanes/tracks aligned by a shared time/step axis.
- Each lane has its own labeled nodes and small functional icons.
- Connectors within a lane = sequential.
- Connectors across lanes = coupling / synchronization, drawn distinctly (e.g. dashed).
- A short legend when coupling notation is non-obvious.
- Independence between lanes must be visually clear.
