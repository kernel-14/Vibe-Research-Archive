Schema layout language:

- Compact card or document-like block.
- Header bar with the schema name.
- Stacked field rows; each row = field name + type + optional tag.
- Tag pills for constraints (required, indexed, etc.) only if the brief mentions them.
- Generous padding; never let labels touch the border.
- One example value at most, only if explicitly requested.
