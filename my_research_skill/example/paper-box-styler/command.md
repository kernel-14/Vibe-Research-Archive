# Command: paper-box-styler

From `example/paper-box-styler/`:

```bash
pdflatex -interaction=nonstopmode -output-directory=build sample.tex
```

The sample uses the default `listings` backend and does not require `-shell-escape`.
